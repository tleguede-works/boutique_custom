Place here the exported fixtures generated by:

```bash
bench --site <site-name> export-fixtures
```

Expected phase 1 content:

- custom field fixtures for `Item-saison` and `Item-collection` if used
- print format fixtures for the POS ticket and item label
- report fixtures for the small retail operating reports

Do not export everything. Keep the fixture set narrow and explicit.
